name: Deploy to App Stores

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  deploy:
    name: Build & Submit to Play Store
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Set version and versionCode from tag
        run: |
          VERSION=${GITHUB_REF_NAME#v}
          VCODE=${VERSION##*.}
          npx json -I -f app.json -e "this.expo.version='${VERSION}'"
          npx json -I -f app.json -e "this.expo.android.versionCode=${VCODE}"

      - name: Install dependencies
        run: npm ci

      - name: Setup Expo
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: Build Android (EAS)
        env:
          EAS_BUILD_NO_EXPO_GO_WARNING: true
        run: eas build --platform android --profile production --non-interactive

      - name: Submit to Play Store (EAS)
        run: eas submit --platform android --profile production --latest --non-interactive
